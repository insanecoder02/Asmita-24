<resources xmlns:tools="http://schemas.andr oid.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Xenon" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Xenon" parent="Base.Theme.Xenon">
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />


    <style name="noNmaej" parent="Widget.AppCompat.Toolbar">
        <item name="title">@string/empty</item>
    </style>

    <style name="Theme.NavigationDrawer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#FFECA1</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.NavigationDrawer.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.NavigationDrawer.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.NavigationDrawer.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.Tets.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="title">@string/empty</item>
    </style>

    <style name="Theme.Tets.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="title">@string/empty</item>
    </style>

    <string name="empty">"Home"</string>
</resources>